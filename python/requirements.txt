# ── Core async ───────────────────────────────
redis>=5.0.0
asyncpg>=0.29.0
aiohttp>=3.9.0

# ── LLM — OpenRouter ─────────────────────────
openai>=1.30.0

# ── Scheduling ───────────────────────────────
apscheduler>=3.10.0

# ── Data / config ────────────────────────────
pydantic>=2.5.0
toml>=0.10.2
structlog>=24.0.0
prometheus-client>=0.20.0

# ── Market data / sentiment ───────────────────
yfinance>=0.2.36
vaderSentiment>=3.3.2

# ── MCP client (Yahoo Finance MCP integration) ─
mcp[cli]>=1.6.0

# ── Chat Agent (Discord + Telegram bots) ──────
discord.py>=2.3.0
aiogram>=3.7.0

# ── Options Monitor ───────────────────────────
matplotlib>=3.8.0

# ── ML Predictor Ensemble ─────────────────────
scikit-learn>=1.4.0

# ── Portfolio Optimization (MVO / Risk Parity) ─
scipy>=1.11.0
