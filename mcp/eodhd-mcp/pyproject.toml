[project]
name = "eodhd-mcp"
version = "0.1.0"
description = "EODHD (EOD Historical Data) market data MCP server"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.0.0",
    "requests>=2.31.0",
]

[tool.setuptools]
py-modules = ["server", "run_http"]
